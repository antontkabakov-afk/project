# Vite development proxy target. The frontend uses /api by default and
# proxies requests to this backend during local development.
VITE_DEV_PROXY_TARGET=http://localhost:5247

# Optional direct API override for non-proxied setups.
# VITE_API_BASE_URL=https://api.example.com/api
